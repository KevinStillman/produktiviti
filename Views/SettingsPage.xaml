<UserControl x:Class="produKtiviti.Views.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>

        <!-- Toggle switch style -->
        <Style x:Key="ToggleStyle" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid VerticalAlignment="Center">
                            <Border x:Name="track"
                                    Width="44"
                                    Height="24"
                                    CornerRadius="12"
                                    Background="{DynamicResource ToggleTrackOffBrush}"/>
                            <Ellipse x:Name="thumb"
                                     Width="18"
                                     Height="18"
                                     Fill="{DynamicResource ToggleThumbBrush}"
                                     HorizontalAlignment="Left"
                                     Margin="3,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="track" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="thumb" Property="Margin" Value="0,0,3,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <StackPanel Margin="40,40,40,40">

        <TextBlock Text="Settings"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   FontSize="28"
                   FontWeight="Light"
                   Margin="0,0,0,32"/>

        <!-- Appearance section -->
        <TextBlock Text="APPEARANCE"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   FontSize="11"
                   FontWeight="SemiBold"
                   Margin="0,0,0,12"/>

        <Border Background="{DynamicResource SidebarBrush}"
                CornerRadius="8"
                Padding="16,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="Dark Mode"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               FontSize="14"/>
                    <TextBlock Text="Switch between dark and light theme"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="12"
                               Margin="0,2,0,0"/>
                </StackPanel>

                <CheckBox x:Name="DarkModeToggle"
                          Grid.Column="1"
                          Style="{StaticResource ToggleStyle}"
                          VerticalAlignment="Center"
                          Checked="DarkModeToggle_Changed"
                          Unchecked="DarkModeToggle_Changed"/>
            </Grid>
        </Border>

    </StackPanel>
</UserControl>
